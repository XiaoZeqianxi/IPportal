import React from "react";

const Register = () => {
    return (
        <>
            <div>
                place a progress component here
            </div>
            <div>
                step 1: choose the type of IPs
                based on the IP category render the corresponding input boxes
            </div>
            <div>
                step 2: place the information input boxes
            </div>
            <div>
                step 3: place an input box or upload box for original file url or upload to db
            </div>
            <div>
                step 4: place a review page of info inputted
            </div>
            <div>
                place a submit button after step 4
            </div>
            <div>
                return a success / failure page of submission
            </div>
        </>
    );
}

export default Register;