import React from "react";

const Policies = () => {
    return (
        <>
            <div>
                place cards to show all policies related
            </div>
            <div>
                place a pagination component to allow more policies if required
            </div>
        </>
    );
};

export default Policies;