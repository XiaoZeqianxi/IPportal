import React from "react";

const Search = () => {
    return (
        <>
            <div>
                place a input bar here
            </div>
            <div>
                after the input bar place a button to invoke search
            </div>
            <div>
                place a container to show the results
            </div>
            <div>
                place a pagination under the results to allow pagination
            </div>
        </>
    );
};

export default Search;